{
 "_purpose": "Canonical 3D-scene vantages for the visual-regression + perf sweeps (scripts/probe-sweep.js). This is the golden spec: each vantage teleports the probe to a fixed camera + time/sea, captures one screenshot, and records fps. The `expect` text is what a correct frame looks like; `regressions` lists the specific failures this vantage exists to catch (each has bitten a real release). Data-driven per the infinite-of-x rule - add a vantage here, both sweeps pick it up. Coordinates are the same debug/camera_request.json + debug/showcase_request.json fields lib.rs polls.",
 "_perf_note": "perf_floor_fps is an advisory lower bound at this vantage on the dev machine (RTX 4070). The perf sweep flags a stop below it; it is a smoke threshold, not a hard gate.",
 "_clock_note": "READ THIS BEFORE PICKING A `time`. The showcase `time` field is a GLOBAL clock, not the local solar hour at the vantage (src/engine/ipc.rs:200-205 feeds it straight into the world clock). The local solar hour is roughly `time + lon/15`. So time 12.0 is local noon only near lon 0; at lon 138.8 (the Fuji vantages) time 12.0 is about 21:15 LOCAL and the frame comes back as a night scene. Pick `time = desired_local_hour - lon/15`, wrapped into 0..24. This trap cost `ground-storm-inslab` its whole purpose from v0.1070 to v0.1073: it was installed as the permanent BUG-049 daylight gate and was silently rendering at night, so every cloud LIGHTING change was being judged in the dark.",
 "vantages": [
  {
   "id": "sahara-noon-ground",
   "desc": "Ground level in the Sahara at local noon, facing the horizon",
   "showcase": {
    "time": "12.0",
    "sea": "0.3"
   },
   "camera": {
    "body": "earth",
    "lat": 23,
    "lon": 13,
    "altitude_km": 0.03,
    "look_offset_deg": 80
   },
   "settle_s": 8,
   "expect": "A deep, saturated blue sky dome fading to a pale warm band at the horizon, over rolling tan sand. Bright and clearly daytime.",
   "regressions": [
    "NO stars visible (v0.913-era daytime star bleed)",
    "no washed-out near-white dome (v0.918 exposure)",
    "no hard-edged black shell or ring around the horizon"
   ],
   "perf_floor_fps": 20
  },
  {
   "id": "ocean-grazing-calm",
   "desc": "8 m above a calm sea, grazing angle to the horizon",
   "showcase": {
    "time": "10.7",
    "sea": "0.3"
   },
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 0.008,
    "look_offset_deg": 88
   },
   "settle_s": 10,
   "expect": "Deep rippled blue water reading as gentle rolling swell all the way to a clean horizon, under a blue-to-warm sky. Fine surface structure, not glassy-flat.",
   "regressions": [
    "NO black/white zebra banding (v0.921 analytic-wave aliasing)",
    "no concentric moire interference rings",
    "no white-out sheet at the grazing angle",
    "no cloud-deck sheet band riding the horizon line (v0.958 low-camera aerial fade; clouds-off A/B 2026-07-25)"
   ],
   "perf_floor_fps": 20
  },
  {
   "id": "ocean-storm-low",
   "desc": "6 m above a storm sea, grazing angle",
   "showcase": {
    "time": "10.55",
    "sea": "0.9"
   },
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 0.006,
    "look_offset_deg": 85
   },
   "settle_s": 8,
   "expect": "Dark blue-grey choppy water with organic white foam streaks riding the wave crests (roughly a third coverage at most). Reads as a rough sea, not a white sheet. SKY: the deck overhead is a few LARGE masses with a defined base that dissolves into the horizon haze band, not a distant field of small uniform puffs (reworded v0.1074, when the High path started marching the slab at its designed 25.5-76.5 km instead of 76-128 km). NOTE: this vantage pins `sea` but NOT `weather`, so the live condition drifts - it captured as Clear at v0.1074. Judge the SEA STATE here, which `sea` does pin; use ground-storm-inslab for storm lighting.",
   "regressions": [
    "no full-white ocean (v0.906 whiteout)",
    "foam on crests only, not flat areas",
    "no zebra banding even in chop",
    "no cloud-deck sheet band riding the horizon line (v0.958 low-camera aerial fade; clouds-off A/B 2026-07-25)"
   ],
   "perf_floor_fps": 18
  },
  {
   "id": "ocean-high-eye",
   "desc": "Open ocean from a 30 m eye - the water-shell COVERAGE case.",
   "expect": "Wave field carries to the horizon. NO flat pale plates: those are the coarse backstop shell showing through where the wave shell ran out of leaves. A high eye sees far more sea than a 3 m eye, so this is where the water leaf budget is actually tested (v0.1048: the operator hit it at 13 m with terrain_split_px = 2). SKY: the cloud deck reads as a few LARGE near masses with a defined, continuous base, subtending tens of degrees. (Reworded v0.1074: the old text was written against a deck the High path was marching at 76-128 km, three times its designed distance, where it read as a remote field of small same-sized puffs under a scalloped edge. The slab now marches at 25.5-76.5 km, so the same weather draws far fewer, far bigger elements.)",
   "regressions": [
    "water-shell coverage / backstop show-through",
    "cross-LOD seams at distance",
    "no remote painted ceiling of many small same-sized puffs - if the deck's elements are small and uniform again, cloud_layer_volumetric has stopped consuming material.params.w and is marching a slab thickness too high (the v0.1025-v0.1073 dead write, fixed v0.1074)"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 0.03,
    "look_offset_deg": 88
   },
   "showcase": {
    "time": "10.7",
    "sea": "0.3"
   },
   "settle_s": 10,
   "hold_altitude": true
  },
  {
   "id": "fuji-forest-ground",
   "desc": "Ground level near Mt Fuji in a conifer forest, morning",
   "showcase": {
    "time": "2.9",
    "sea": "0.3"
   },
   "camera": {
    "body": "earth",
    "lat": 35.3,
    "lon": 138.8,
    "altitude_km": 0.03,
    "look_offset_deg": 80
   },
   "_settle_note": "75 s, NOT the 25 s this carried through v0.1078. MEASURED 2026-07-31: at 25 s this vantage captures 43-48 ms while the scene is still streaming, and it keeps getting heavier for another ~40 s before it settles at 93.7 ms +/- 5.8 (10.7 fps) on the operator's max-quality rig config. So the sweep was reporting 21.3 and 18.8 fps and passing an 18 fps floor at 55% of the real number. Every perf figure taken here before v0.1079 is measuring a half-built forest. probe-sweep.js `settle_s` is a plain sleep (scripts/probe-sweep.js:236) with no steady-state detection; a proper fix waits for [PatchBatch] to stop changing in run.log, which is a rig change, not a data change.",
   "settle_s": 75,
   "hold_altitude": true,
   "_hold_altitude_note": "Added v0.1081. Without it the walking player comes to rest on different ground every run - [NearTree] recompute reported alt 1240 / 1364 / 1376 / 1424 m across four runs of a byte-identical config, frame time swung 39.7 to 85.3 ms (2.15x), and 2 of those 4 runs failed to capture at all (\"screenshot: null\" / \"camera: null\"). camera_request parks the player but leaves gravity acting during the 75 s settle; the re-park at scripts/probe-sweep.js:254 pins the pose once the patches are built. fuji-forest-hillside already carried this key.",
   "expect": "Dense green ground with real 3D conifer trees standing around the camera - visible brown trunks, needled canopies - and BAKED SILHOUETTE cards, ragged crown outlines with sky visible through their edges, never plain rectangles, carrying the forest toward the horizon. Grass underfoot. BACKLIT FOLIAGE GLOWS: crowns seen against the sky read as luminous yellow-green with visible leaf structure, never as black shards. CROWNS ARE OPAQUE MASSES (v0.1088, cluster cards): a crown seen against the sky blocks most of it, and what occludes is FOLIAGE, not branch. Sakura in bloom reads as bare branches SLEEVED in blossom, not a pink haze: the pink mass follows the branch lines.",
   "regressions": [
    "3D tree MODELS present, not just flat cards (v0.923 default-off regression)",
    "no bare ground where a forest should be (bare-forest card-hide bug)",
    "trunks visible, not floating canopies",
    "NO flat rectangular slabs anywhere in the tree field: every card past the 3D-model ring shows a crown outline with sky visible through its edge. 6 of the 8 rows in data/vegetation/trees.ron are procedural (model: \"\") and had NO atlas tile, so they fell through planet_chunks.rs:1925 to plain coloured trunk+canopy rectangles; fir and pine took the sprite path but a SHIPPED build has no assets/models/ (.github/workflows/build-desktop.yml copies data/ + assets/icons/ + assets/shaders/ only), so the bake aborted, tree_atlas_ready stayed false and their cards painted the flat green fallback at 90-fragment-main.wgsl:824. Photographed as a picket fence of grey slabs at v0.1078.",
    "NO storm-strength lean in calm weather: the same wind uniform (light7_cone_inner, wind_dir_world.xyz + speed m/s) must produce a near-upright stand here. Crowns sway but stay centred over their bases.",
    "NO black backlit canopy. In the crop (900,0)-(1700,500) the MEDIAN luminance of canopy pixels must be at least 0.10x the median of sky pixels in the same crop, and fewer than 30% of canopy pixels may fall below luma 16. MEASUREMENT: luma = 0.2126R + 0.7152G + 0.0722B; a pixel is SKY if B > R and B > G (the dome is blue-dominant, foliage never is) and CANOPY otherwise - note that lumps trunks, branches and deep interior gaps in with the leaves, which is why the dark-fraction half of the gate is 30% and not 20%. MEASURED v0.1080.1 (before): ratio 0.026, dark fraction 58.5% (the finding's own slightly different classifier read 0.063 / 53.9% on its capture). MEASURED v0.1081 (after): ratio 0.448, dark fraction 22.8%, same rig, same config, same crop. Cause of the failure: tree_mesh::blade() emitted through PlantMeshBuilder::tri2, which never sets self.organ, so every foliage face on sakura/momiji/oak/birch/acacia (and every blossom) carried Organ::Stem, bit 19 was clear, 90-fragment-main.wgsl:1157 is_leaf was false, and the canopy was shaded by the BARK branch at :1259 with no transmission at all. Palm was the only correct species. The leaf-flutter gate at 00-bindings-vertex.wgsl:682 reads the same bit, so v0.1080 foliage wind had no flutter on any procedural tree either. MEASURED AT 2560x1387, AND THIS GATE IS RESOLUTION DEPENDENT: the residual dark is geometric aliasing of sub-pixel leaf blades (no MSAA - every MultisampleState in src/renderer/pipeline.rs is count 1), so the SAME v0.1081 build measures 0.448 / 22.8% at 2560x1387 and 0.211 / 35.3% at 1280x720. The ratio half passes at both widths; the dark-fraction half passes at 2560 and FAILS at 1280. Judge this only on a 2560-wide capture and record the capture width in the manifest - a second rig running concurrently can silently change the window size, which is exactly how the 1280x720 reading was produced.",
    "CARD/MODEL SIZE MATCH at the handoff: capture the same acacia or palm from just inside and just outside tree_model_distance (120 m). The silhouette height must agree within 10% and the trunk base must sit on the ground in both, with NO vertical hop as the camera crosses the ring. Cards are emitted h x h (planet_chunks.rs:1717 `let w = h;`) while the baker frames a square on max(width, height) and returns a footprint the atlas builder discards as _fp (billboard_bake.rs:167-171 and :103), so a crown WIDER than the tree is tall renders about 27% too short with its trunk base about 13% of the card height off the ground. Acacia is that case by construction (tree_mesh.rs:545, \"the crown is WIDER than the tree is tall\"); fir and pine are height-dominant, which is why the defect was invisible while only they took the sprite path. This is a MOTION gate - walk the ring, do not just compare two stills.",
    "FIR AND PINE MUST BE VISIBLE IN A SHIPPED BUILD. They are invisible today (audited at v0.1085, from source, all three legs confirmed) at EVERY distance, and THIS RIG CANNOT SEE IT. (1) The near-model builder generates a procedural mesh only for rows where is_procedural() (src/lib.rs:9382); fir/pine carry model: \"fir_sapling\" / \"pine_sapling_small\" so they get none, their glTF is absent from a release bundle (.github/workflows/build-desktop.yml copies data/ + assets/icons/ + assets/shaders/ ONLY - assets/models/ and assets/textures/ never ship), and the draw loop `continue`s on the cache miss (src/lib.rs:9646). (2) The card baker skips them the same way, logs \"[Bake] fir v0: no model parts (assets/models/ absent?), tile N left empty\" and leaves the atlas tile transparent (src/renderer/billboard_bake.rs:297-306). (3) The sprite branch then discards every texel of that empty tile at `spr.a < 0.5` (assets/shaders/pbr/90-fragment-main.wgsl:833). Nothing errors, because tree_atlas_ready stays true on the 6 procedural species that do bake. CONSEQUENCE, from the elevation gates in data/vegetation/trees.ron: at this vantage (lat 35.3, ~1245 m) fir+pine are 2.0 of the 8.2 passing weight, so ~24% of the stand is missing; above 1600 m at this latitude and above 2000 m anywhere on Earth, fir and pine are the ONLY species whose gates pass, so a shipped build shows a COMPLETELY BARE alpine forest up to the 3000/3200 m treeline. The rig junctions the whole repo assets/ into the sandbox (scripts/probe-sweep.js:90), so every sweep runs with 62 photoscans present that no player has. Judge this only in a --shipped-assets rig that junctions assets/icons and assets/shaders alone. FIX: build a procedural form mesh for every registry row - tree_mesh::build_tree already dispatches purely on def.form (src/renderer/tree_mesh.rs:441-447) and fir/pine already carry form: \"conifer\" - and fall back to proc:{id}_v{va} when the glTF key is absent.",
    "CROWN GAP FRACTION (v0.1088, cluster cards). On an isolated crown against sky, measure per-column chords (topmost to bottommost non-sky pixel): sky-through-crown must be <= 0.30 and the wood/dark-to-foliage ratio <= 0.35:1. Real behaviour is Beer-Lambert for canopies, gap = exp(-K*LAI/cos(theta)) with K ~= 0.5, so a cherry at LAI 3 transmits 22% and an oak at 4.5 transmits 10%; visible woody area is 10-20% of plant area in a leafed crown. MEASURED v0.1087.1 at 1280x720, box (790,40)-(990,175): gap 32.6%, foliage 30.5%, wood/dark 37.0%, ratio 1.21:1; the whole canopy band (0,0)-(1280,200) read 2.89:1. Root cause: the geometric blade layer alone delivers LAI 0.31-0.50 (sakura 9.9 m2 of leaf, oak 48.4 m2, both derived from the generator itself), i.e. 5-10x short, and closing it with blades would cost ~60,000 triangles per tree against a MAX_TRIS of 8600. RE-BASELINE AT 2560 BEFORE THIS BECOMES A GATE - like the backlit-canopy gate above, it is resolution dependent. CI TWIN (runs without a GPU): renderer::tree_mesh::tests::cluster_cards_reach_target_lai_and_fit_the_budget asserts the card layer delivers the species' target_lai within 25% and still fits MAX_TRIS.",
    "CARD SUN/SHADE SPLIT (v0.1088). Within one crown, the mean luma of the sun-facing third must exceed the away-facing third by >= 1.8x, and no card may render as a uniform flat patch - the SD of luma WITHIN a single card's screen footprint must exceed 6/255. A card lit by its own flat quad normal scores 1.0x and 0: every card with the same facing renders at identical brightness, with hard steps where two cards abut. Cluster cards carry per-corner normals blended 0.70 toward the outward direction from the CLUSTER centre and then 0.30 toward the outward direction from the CROWN centre, so a tuft shades as a ball and the crown as a lit sphere. NOTE the far-field card path does the OPPOSITE on purpose (planet_chunks.rs:1686/:1727 pin card normals to the radial up, the v0.896 black-slab fix), which is why a distant forest reads as a flat green carpet - do not 'fix' the near cards by copying it. CI TWIN: renderer::tree_mesh::tests::card_normals_are_spherified_not_flat.",
    "CROWN AMBIENT GRADIENT AND BACKLIT SATURATION (v0.1088). (1) The mean luma of a crown's top third must exceed its bottom third by >= 1.8x on the SAME crown. (2) The brightest decile of green-dominant canopy pixels must hold saturation (max-min)/max >= 0.45: a backlit leaf is lit mostly by the beam transmitted THROUGH it, chlorophyll transmits selectively in the green, so transmitted light is MORE saturated than the leaf's own reflectance. MEASURED v0.1087.1 at 1280x720: canopy band saturation 0.302, right crown 0.269, against leaf albedo saturation 0.55 (sakura) / 0.62 (oak) in trees.ron, with 0% clipped pixels - so this is full achromatic sky ambient landing on every leaf regardless of depth in the crown, NOT a tonemap problem. Top/bottom-third luma read 1.30/0.92/0.65/0.67 across four boxes, i.e. ~1.4x at best. Fix rides on a per-card baked AO scalar (ao = exp(-k*depth-inside-the-crown-envelope), interior landing at 0.20 of the shell) multiplying the AMBIENT term ONLY, never the sun term - the sun already has the shadow map. RE-BASELINE AT 2560. CI TWIN: renderer::tree_mesh::tests::cluster_ao_darkens_the_crown_interior.",
    "CANOPY CASTS DAPPLE, NOT RECTANGLES (v0.1088). On open ground under a crown the 8x8 px tile-mean SD must exceed 6/255 (MEASURED v0.1087.1: 1.3 at 2-4 m and 1.9 at 3-8 m - a flat carpet, no flecks at all), and NO shadow boundary in the frame may be a straight line longer than 40 px at a non-terrain angle. A straight-edged shadow means alpha-cutout foliage is stamping its FULL QUAD into the shadow map, i.e. the foliage draw is going through the depth-only shadow pipeline (src/renderer/pipeline.rs:566 and :711, both fragment: None, so there is no alpha test at all) instead of an alpha-testing variant. That would be visibly WORSE than today's no-dapple state and will read as a bug the moment the operator walks under a tree. The shadow ortho box is 1.5 km at ~0.73 m/texel, so a 0.5 m cluster is about one texel: expect soft blobby flecks, not crisp ones.",
    "BLOSSOM MORPHOLOGY AND PLACEMENT (v0.1088). On the nearest sakura, connected components of blossom-coloured pixels must have a median area consistent with a 3-8 cm flower cluster at that distance, not a 1-3 px speck. Prunus x yedoensis: 3.5 cm flowers, five NOTCHED petals, 3-6 per umbel, umbels a few centimetres apart along the twig, opening BEFORE the leaves. Through v0.1087 the blossom was a single 0.090 m pink triangle (2.6x life size, wrong shape) scattered through a 0.74 m ball, so a cherry rendered as a green-and-pink dust cloud. ALSO: 'NO GREEN-AND-PINK MIX on a species with blossom_frac > 0.5' - leaf-coloured foliage must be under 10% of the crown's foliage pixels while in bloom. CI TWINS (the placement half is not reliably measurable from a screenshot): renderer::tree_mesh::tests::cluster_cards_sleeve_the_twigs_they_belong_to (every card centre within 0.45 card-widths of a real limb segment), ::a_blooming_species_keeps_its_leaf_layer_a_minority, ::blossom_data_matches_a_real_cherry.",
    "CANOPY OVERDRAW, AND MEASURE IT AT 2560 (v0.1088). Alpha-tested cluster cards STACK, and unlike triangles that cost scales with RESOLUTION: at 2560x1387 the frame is 3.85x the pixels of the 1280x720 the vegetation A/B set was measured at, so headroom that survives at 720p need not survive there, let alone on the cheap hardware this project targets. Budget the crown as OVERDRAW, not as triangles. The generator reports the expected figure every CI run (LAI / mean sprite coverage): 5.7 alpha-test layers per canopy pixel for sakura at v0.1088. Re-run the vegetation A/B set (veg_density 0.6 -> 0.1, veg_tree_card_m 1500 -> 3000, sun_shadows off, terrain_split_px 2 -> 4) AT 2560 when the near cards land; at 1280x720 on an RTX 4070 every vegetation manipulation measured INSIDE the +/-11% run-to-run band, so this arc's cost is invisible at that width.",
    "NEAR/CARD HANDOFF IS INVISIBLE, and tree_card_hide_m must TRACK COVERAGE. Walk from 1 m to 8 m from a single tagged tree: crown silhouette height and width must agree within 8% across the geometry/card switch and no foliage element may pop, appear or vanish at the boundary (a MOTION gate, like the 120 m CARD/MODEL SIZE MATCH entry above). SEPARATELY, [TreeHandoff] in run.log must report hide within ~10 m of covered on every line. MEASURED 2026-08-01 at v0.1087.1: 'near=600 drawn=256 covered=119m hide=22m' on one config and 'covered=120m hide=121m' on another with identical model settings - the 22 m reading means every tree from 22 m to 119 m drew its full ~8,250-triangle model AND its silhouette card in the same frame (~250 trees at that density). src/lib.rs takes hide_m from state.near_trees[NEAR_TREE_DRAW_BUDGET-1] (a list-index probe) while the loop two lines earlier already accumulated covered_r2, the true reach of the set that actually drew; the block's own comment describes the intended value as 'the actual reach of the drawn set, slightly shrunk', i.e. covered_r2.sqrt() - 8.0. Until that is fixed, every crown measurement in this list is taken through a card pasted over a model.",
    "NO OPEN TWIG TIPS: no terminal limb may show a flat elliptical cut face against the sky. PlantMeshBuilder::tube emits no end cap and the v0.1086 weld only plugs junctions where a CHILD buries itself in a parent, so a terminal tip is an open pipe you can look down. Photographed at v0.1087.1 in .probe-rig-opt/crop_crown_raw.png. The cluster-card emitter places a CAP card just beyond every terminal tip, facing along the twig, for exactly this reason."
   ],
   "_fidelity_note": "2026-08-01 (v0.1085 vegetation domain pass): this vantage was measured in detail - canopy/sky median-luma ratio 0.237, 36.7% of solid canopy pixels both dark (luma<30) and achromatic (chroma<12), crown top-third/bottom-third luma 1.30 / 0.92 / 0.65 / 0.67 across four boxes, forest-floor 8x8 tile-mean SD 1.3 (2-4 m) and 1.9 (3-8 m), plus leaf-normal statistics from a bit-exact replica of the generator RNG (74.4-78.3% of camera-facing blade faces are undersides against 61.2% for a spherical leaf-angle distribution). DO NOT paste any of those numbers into the gates above. Two independent reasons: the capture was 1280x720 while the gates here are specified at 2560 wide and the backlit-canopy gate is provably resolution dependent (0.448/22.8% at 2560 vs 0.211/35.3% at 1280 on the SAME build), and the frame contained 62 photoscan fir/pine that a shipped build does not have (see the FIR AND PINE regression above). Re-baseline at 2560 in a --shipped-assets rig before any of it becomes a gate.",
   "perf_floor_fps": 9,
   "_perf_floor_note": "Restated 18 -> 9 at v0.1081. The 18 was written against 25 s captures of a half-streamed forest; this vantage's own _settle_note records the settled truth as 93.7 ms +/- 5.8 = 10.7 fps, and fresh v0.1080.1 captures returned 10 fps. The floor was already violated before anyone touched foliage, so at 18 the next writer to add a shader term gets blamed for a pre-existing miss. 9 fps is a real floor under the measured settled state. Do NOT use this vantage for A/B frame-time work even now that hold_altitude pins the pose - use ground-storm-inslab (50.7 +/- 1.8 ms, n=3), which is the reproducible perf vantage. CAVEAT ADDED v0.1081.1, READ BEFORE TRUSTING ANY NUMBER IN THIS NOTE: src/lib.rs:2882 clamps the frame delta with .min(0.1) and the SAME clamped dt stamps the perf counters at :11551 and :11555, which src/engine/ipc.rs:549-550 writes into screenshot_done.json and scripts/probe-sweep.js:269-270 records. fps therefore saturates at exactly 10.0 and frame_ms at exactly 100.0, so a 9 fps floor CANNOT FAIL and every figure above (93.7 ms, 10.7 fps, 50.7 ms) is a censored sample biased toward 100 ms. Timed off the log instead, between consecutive 60-frame [ChunkDiag] lines (src/lib.rs:8909), this forest runs about 131 ms / 7.6 fps where the rig reported 99.1. Unclamp the counters (keep the clamp for the simulation dt, stamp fps and the ring from a raw_dt, and report the ring's MEDIAN alongside its mean), then re-measure and restate this whole note. See docs/design/vegetation-cards-every-species.md precondition P2."
  },
  {
   "id": "fuji-forest-hillside",
   "desc": "About 350 m above a forested Fuji slope, looking down across the canopy. The CARD rung seen from above, which is where the operator's \"black squares in a grid\" verdict came from. Added 2026-07-31: there were 25 vantages and exactly one forest one, and both forest vantages sat at 30 m INSIDE the stand, so the 70 m-to-cutoff card stage - the largest-screen-area vegetation LOD at any elevated view - had no clean rig coverage at all.",
   "showcase": {
    "time": "2.9",
    "sea": "0.3",
    "weather": "clear"
   },
   "_weather_note": "clear is PINNED (v0.1082). Without it the runtime verifier's storm run captured a total grey whiteout from inside the cloud deck at this altitude - two of three verification runs were unusable for canopy judgement because showcase only set time and sea. Any elevated vantage must pin weather.",
   "camera": {
    "body": "earth",
    "lat": 35.3,
    "lon": 138.8,
    "altitude_km": 0.6,
    "look_offset_deg": 55
   },
   "_camera_note": "altitude_km 0.15 -> 0.6 and look_offset_deg 72 -> 55 (v0.1082, after a 1.0/52 detour). The vantage AS FIRST WRITTEN NEVER GOT ABOVE THE CANOPY: at altitude_km 0.15 it captured \"Alt: 0 m\" and framed identically to fuji-forest-ground, so all three of its above-canopy gates were unevaluable and read as passing only because nothing evaluated them. poll_camera_request (src/engine/ipc.rs) parks above a SAMPLED ground radius which under-reads the DRAWN Fuji surface by about 320 m. Transfer function measured at this lat/lon: requested 0.15 -> Alt 0 m, 0.30 -> 0, 0.60 -> 353, 1.20 -> 880. Over water the same path is exact (ocean-700m captures Alt 713 m), so this is land-only. WHY 0.6 AND NOT 1.0: the runtime verifier flew the 1.0/52 aim (834 m eye) and its one clear frame showed a BARE slope with zero trees, because at that altitude the lower-two-thirds band sits BEYOND the 1500 m card cutoff (independently confirming the NO-VEGETATION-RING defect). 0.6/55 is the MEASURED pose that produced the crown-structure baselines with canopy visibly in frame at a 353 m eye - use the pose that was measured to work, not an interpolated one.",
   "hold_altitude": true,
   "settle_s": 75,
   "expect": "A forested slope filling the lower two-thirds of the frame, seen from well above the canopy: individual crowns readable as separate ragged silhouettes with a clear sunlit-top / shaded-flank split, the canopy mass reading DARKER than the open ground beyond it. (Forest-to-the-horizon is NOT expected yet: the 1500 m card cutoff hard-ends the vegetation - that is the NO-VEGETATION-RING gate's defect, fixed by the Bayer fade, and only after that fix should this expect be upgraded. look_offset_deg is measured from straight down, so 55 aims about 35 degrees below the horizon. showcase `time` is a GLOBAL clock; local solar hour = time + lon/15, so lon 138.8 needs time 2.9 for local noon - see _clock_note.)",
   "regressions": [
    "THE VANTAGE MUST ACTUALLY BE ABOVE THE CANOPY: the HUD Alt readout must exceed 250 m (trees are ~20 m; 353 m measured at this pose) and no trunk base may appear at eye level. MEASURED 2026-07-31 at v0.1081: requested altitude_km 0.15 captured Alt 0 m and framed identically to fuji-forest-ground, because the camera parks above a SAMPLED ground radius that under-reads the drawn Fuji surface by about 320 m (0.15 -> 0, 0.3 -> 0, 0.6 -> 353, 1.2 -> 880 m measured at this lat/lon; over water the same path is exact, ocean-700m captures 713 m). Every above-canopy gate below was unevaluable until the altitude was raised. If a future rig change makes altitude exact, this gate still holds; if a capture comes back at eye level, do not judge anything else in this list from it.",
    "NO flat rectangular slabs in the canopy band - the finding-1 gate, at the distance where it is most visible",
    "the forest band's mean luminance must be BELOW that of the adjacent open ground (real canopy albedo 0.08-0.15 vs grass 0.20-0.25). Card shading normals were pinned to the radial up at planet_chunks.rs:1686 and :1727 (the v0.896 black-slab fix), so cards lit exactly like the terrain and a distant forest read as a flat green carpet painted on the hillside. MEASURED at v0.1081 from an 880 m eye: the forest disc read 29.9 against open slope 79.2, so the albedo half of this was already right - it is the crown-scale STRUCTURE gate below that fails.",
    "CROWN-SCALE STRUCTURE (the \"flat carpet\" gate, given a number at v0.1081.1; threshold restated v0.1082 at the conditions it was MEASURED at): in the canopy band the stddev of 16x16 px TILE MEANS must be at least 15/255 on a 1280x720 capture in box (350,300)-(950,500) - scale the box and tile size proportionally at other widths, and re-baseline before judging at 2560 - and per-tree luminance variance in the band must exceed 8/255. MEASUREMENT: luma = 0.2126R + 0.7152G + 0.0722B; tile the canopy box into 16x16 px blocks, take each block's mean, take the stddev of those means. MEASURED 2026-07-31 at v0.1081 (1280x720, box 350,300-950,500, 353 m eye): 4.93 for the card band against 47.29 for the 3D-model band in the same session, a 9.6x deficit at crown scale - the card canopy holds one tenth the crown-scale contrast of the real canopy it hands off from. Multi-scale for reference, card band vs model band at 1/2/4/8/16/32 px: 14.97/11.28/8.81/6.65/4.93/4.16 against 72.55/64.23/58.80/53.78/47.29/39.66; all of the card band's variance is pixel-scale noise.",
    "NO VEGETATION RING at the card cutoff: across the cutoff radius on a single continuous slope, the mean luminance step must be under 15% and the local (8x8 px) SD ratio under 2.0. MEASURED 2026-07-31 at v0.1081 from an 880 m eye: inside 29.9 mean luma / local SD 8.13, outside 45.5 / 1.18 - a 34% step and a 6.9x texture step, with the boundary tracking the camera. The card stage hard-discards with no fade at 90-fragment-main.wgsl:805 (sprite branch) and :842 (coloured branch) on tree_card_far_m, 1500 m; the fix is the 4x4 Bayer dissolve already used for grass at :853-866, over the last ~250 m of the card range.",
    "NO black slabs either: the v0.896 failure this replaced. A card normal blended toward the card facing must never reach N.L = 0 under an overhead sun, and a two-sided card must not go dark on its back face"
   ]
  },
  {
   "id": "limb-400km",
   "desc": "400 km over the Sahara, the atmosphere limb in frame against stars",
   "showcase": {
    "time": "12.0",
    "sea": "0.3"
   },
   "camera": {
    "body": "earth",
    "lat": 23,
    "lon": 13,
    "altitude_km": 400,
    "look_offset_deg": -20
   },
   "settle_s": 8,
   "expect": "The curved planet surface below a THIN bright blue atmosphere limb that fades smoothly into a black star-filled sky. Stars visible right down to the edge of the atmosphere band.",
   "regressions": [
    "NO hard-edged opaque black shell/ring around the planet (v0.925 orbital occlusion bug)",
    "the limb fades to stars, it does not cut off",
    "no straight white cloud slivers hovering ABOVE the atmosphere limb against black sky, and no deck jump between a 390 km and a 420 km capture (the drawn-shell ratio flips at ~399 km; before the v0.1074 slab fix the High path ignored that ratio, so the deck sat at 76-128 km below the flip and snapped back into the atmosphere above it)",
    "CROWN-SCALE STRUCTURE, TIGHTENED (v0.1088). MEASURED v0.1087.1 at 1280x720, box (350,300)-(950,500): 16x16 tile-mean SD = 6.19, and multi-scale at 4/8/16/32 px = 11.34/8.15/6.19/5.29. Variance decaying like pixel noise is the signature of CONFETTI rather than crowns - the open slope in the same frame reads 5.97/5.93/5.52/4.63, so the forest has barely more structure than bare grass. The gate stays >= 15 at 16 px, and ADD: SD(32px)/SD(4px) must exceed 0.55 so the variance lives at crown scale and not at pixel scale (v0.1087.1: 0.47). Re-baseline at 2560 before gating.",
    "CANOPY DOES NOT CRAWL (v0.1088, cluster-sprite mips). With the camera pinned (hold_altitude) and weather pinned clear, capture two frames 1.0 s apart; in the canopy box the mean absolute per-pixel luma difference must be under 4/255 with wind speed under 1 m/s. Above that, a cutout is flickering because it is being point-sampled at mip 0: there are NO mipmaps anywhere in this engine (every texture is mip_level_count 1, every sampler mipmap_filter Nearest, and the tree atlas is read with textureSampleLevel(..., 0.0)), so a 256 px sprite drawn 6 px wide samples about 1 texel in 40 and the alpha test result changes on sub-pixel camera motion. Cluster sprites therefore ship as their OWN mipped textures with per-level alpha-coverage rescale, NOT as tiles of the 6x8 tree atlas (a 2D atlas cannot be mipped without tile-border bleed, and converting it to a texture_2d_array would be a binding-TYPE change on a shared layout, the v0.1029-v0.1038 incident class). CI TWIN: renderer::billboard_bake::tests::cluster_mip_chain_preserves_alpha_coverage asserts every level's above-cutoff coverage stays within 5% of mip 0's (within one texel below 16x16)."
   ],
   "perf_floor_fps": 25
  },
  {
   "id": "fly-through-40km",
   "desc": "Level flight at 40 km looking at the horizon - the camera is INSIDE the cloud slab. The altitude that proves the deck is where the constants say it is; it was the ladder step that exposed the v0.1025-v0.1073 dead write.",
   "expect": "At 40 km the camera is INSIDE the cloud layer: cloud masses pass AT eye level, with the blue atmosphere limb and the ocean visible past them. Nothing renders BELOW eye level by construction: below-horizon rays get no cloud-shell fragment (the shell backface is depth-rejected behind the ocean), so judge the at-and-above band only (critic-measured v0.1074: the change stops dead at the eye-level row). No cloud sits above the black-sky boundary.",
   "regressions": [
    "NO cloud ceiling hanging above the camera at 40 km with the atmosphere limb, stars and ocean all BELOW it (v0.1025-v0.1073 dead-write: cloud_layer_volumetric never consumed material.params.w, so the slab marched at 76-128 km instead of 25.5-76.5 km at every altitude under ~400 km)",
    "no flat white sheet with a straight horizontal top edge (the drawn-shell crossing artifact, visible at 110 km before the fix)"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 40,
    "look_offset_deg": 90
   },
   "showcase": {
    "time": "10.7",
    "sea": "0.3"
   },
   "settle_s": 10
  },
  {
   "id": "blue-marble-12000km",
   "desc": "12,000 km from Earth, the approved full-disc blue marble",
   "camera": {
    "body": "earth",
    "altitude_km": 12000
   },
   "settle_s": 8,
   "expect": "The full Earth disc as a blue marble with a soft blue atmosphere rim, oceans and continents visible, against stars. The operator-approved from-orbit look.",
   "regressions": [
    "disc and rim look unchanged from the approved v0.918 baseline",
    "no white-out, no missing atmosphere rim"
   ],
   "perf_floor_fps": 30
  },
  {
   "id": "australia-limb-12000km",
   "desc": "Australia near the disc edge from 12,000 km - terrain must stay readable through the limb haze (2026-07-25 field report)",
   "camera": {
    "body": "earth",
    "lat": -5,
    "lon": 130,
    "altitude_km": 12000
   },
   "settle_s": 8,
   "expect": "Earth disc with Australia in the lower part, its southern coastline visible toward the disc edge through progressively bluer haze. The continent must read as LAND (tan/brown) well toward the limb, not vanish into flat atmosphere blue.",
   "regressions": [
    "southern Australia readable as land, not replaced by uniform blue",
    "atmosphere rim itself still present and soft"
   ],
   "perf_floor_fps": 30,
   "showcase": {
    "time": "4.2"
   }
  },
  {
   "id": "moon-surface-200m",
   "desc": "200 m over the Moon's cratered surface, sunlit",
   "camera": {
    "body": "moon",
    "lat": 5,
    "lon": -150,
    "altitude_km": 0.06,
    "look_offset_deg": 55
   },
   "settle_s": 15,
   "expect": "Continuous grey cratered lunar regolith with real crater bowls and rims and the true satellite-imagery albedo, under a black star sky. NOT bare flat facets.",
   "regressions": [
    "real cratered terrain, NOT the bare uniform icosphere (v0.919 synthesized-heightmap fix)",
    "no km-wide flat facets with hard creases"
   ],
   "perf_floor_fps": 25
  },
  {
   "id": "sunset-over-water",
   "desc": "Low altitude over water aimed at the setting sun",
   "showcase": {
    "time": "16.85",
    "sea": "0.3"
   },
   "camera": {
    "body": "earth",
    "lat": 30.8,
    "lon": 18.5,
    "altitude_km": 0.012,
    "aim": "sun"
   },
   "settle_s": 6,
   "expect": "A warm reddened sun low over water with a red/orange ember band along the horizon fading up into deepening blue and emerging stars. Sunlight visibly reddened.",
   "regressions": [
    "sun disc reddened not white (v0.915 transmittance)",
    "no washed dome, no black shell above the glow"
   ],
   "perf_floor_fps": 12
  },
  {
   "id": "ocean-150m",
   "desc": "Open ocean from 150 m - the altitude band where the operator sees far-field plates.",
   "expect": "Wave field to the horizon, no flat facets.",
   "regressions": [
    "water-shell far-field coverage"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 0.15,
    "look_offset_deg": 85
   },
   "showcase": {
    "time": "10.7",
    "sea": "0.3"
   },
   "settle_s": 12,
   "hold_altitude": true
  },
  {
   "id": "ocean-700m",
   "desc": "Open ocean from 700 m - the altitude band where the operator sees far-field plates.",
   "expect": "Wave field to the horizon, no flat facets.",
   "regressions": [
    "water-shell far-field coverage"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 0.7,
    "look_offset_deg": 85
   },
   "showcase": {
    "time": "10.7",
    "sea": "0.3"
   },
   "settle_s": 12,
   "hold_altitude": true
  },
  {
   "id": "ocean-underwater",
   "desc": "Submerged, looking down at the SEA FLOOR. (camera_request adds a ~+26 m radial offset from the ship-local camera height, so the requested altitude must overshoot.)",
   "expect": "Bathymetric relief shading with its own smooth normals. NO diagonal swell banding painted on the floor - that was the seabed being flagged water and shaded as an ocean SURFACE (fixed v0.1049).",
   "regressions": [
    "water shell back-face visibility",
    "coarse-leaf floor opening holes underwater"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": -0.12,
    "look_offset_deg": 25
   },
   "showcase": {
    "time": "10.7",
    "sea": "0.3"
   },
   "settle_s": 10,
   "hold_altitude": true
  },
  {
   "id": "ocean-glass",
   "desc": "Sea state pinned to glass (wind ~0.5 m/s).",
   "expect": "A near-mirror sea: almost no chop, the sun path collapses toward a single hard highlight.",
   "regressions": [
    "wind-driven ocean spectrum"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 0.02,
    "look_offset_deg": 87
   },
   "showcase": {
    "time": "10.7",
    "sea": "0"
   },
   "settle_s": 26,
   "hold_altitude": true
  },
  {
   "id": "ocean-storm-high",
   "desc": "Sea state pinned to storm (wind ~25 m/s).",
   "expect": "Big steep seas with whitecaps - visibly a different ocean from ocean-glass, which is the proof the spectrum is wind-driven.",
   "regressions": [
    "wind-driven ocean spectrum"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 0.02,
    "look_offset_deg": 87
   },
   "showcase": {
    "time": "10.7",
    "sea": "1"
   },
   "settle_s": 26,
   "hold_altitude": true
  },
  {
   "id": "desert-night",
   "desc": "Sahara at night, low eye - the terrain terminator gate.",
   "expect": "Uniformly dark ground under starlight. NO bright band toward the sunset azimuth: that was a below-horizon sun still lighting normal-map facets, because the celestial pass has no terminator test (v0.1052).",
   "regressions": [
    "night terrain lit by a below-horizon sun"
   ],
   "camera": {
    "body": "earth",
    "lat": 23,
    "lon": 13,
    "altitude_km": 0.03,
    "look_offset_deg": 84
   },
   "showcase": {
    "time": "22.0",
    "sea": "0.3"
   },
   "settle_s": 12,
   "hold_altitude": true
  },
  {
   "id": "ocean-storm-horizon",
   "desc": "Storm sea from a LOW eye, aimed flat at the horizon - do tall crests occlude the sky?",
   "expect": "Near wave crests break the horizon line and stand against the sky. A ruler-straight horizon over a 10 m sea means the atmosphere shell is compositing over the crests (fixed v0.1053). SKY: the deck is a single large near mass with a defined base sitting well above the horizon line, not a thin remote band riding it (reworded v0.1074 with the slab at its designed 25.5-76.5 km). NOTE: this vantage pins `sea` but NOT `weather` - it captured as Fog at v0.1074, which greys the whole frame and makes the crest judgement impossible. If the capture comes back fogged, re-shoot; the crest test needs clear air.",
   "regressions": [
    "sky shell drawn over near wave crests"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": 0.006,
    "look_offset_deg": 90
   },
   "showcase": {
    "time": "10.7",
    "sea": "1.0"
   },
   "settle_s": 26,
   "hold_altitude": true
  },
  {
   "id": "land-fog",
   "desc": "Sahara ground under fog - does the AIR change?",
   "expect": "Visibility collapses to tens of metres with a condition-appropriate tint. Before v0.1059 weather.visibility reached no shader at all and the air was identical in every condition.",
   "regressions": [
    "weather-driven fog"
   ],
   "camera": {
    "body": "earth",
    "lat": 23,
    "lon": 13,
    "altitude_km": 0.03,
    "look_offset_deg": 80
   },
   "showcase": {
    "time": "12.0",
    "sea": "0.3",
    "weather": "fog"
   },
   "settle_s": 10
  },
  {
   "id": "land-sandstorm",
   "desc": "Sahara ground under sandstorm - does the AIR change?",
   "expect": "Visibility collapses to tens of metres with a condition-appropriate tint. Before v0.1059 weather.visibility reached no shader at all and the air was identical in every condition.",
   "regressions": [
    "weather-driven fog"
   ],
   "camera": {
    "body": "earth",
    "lat": 23,
    "lon": 13,
    "altitude_km": 0.03,
    "look_offset_deg": 80
   },
   "showcase": {
    "time": "12.0",
    "sea": "0.3",
    "weather": "sandstorm"
   },
   "settle_s": 10
  },
  {
   "id": "waterline-split",
   "desc": "Camera AT the waterline over a shallow shelf - the over-under split.",
   "expect": "One frame containing both: air and sky above the surface line, extinguished blue-green water below it, with the water surface itself crossing the screen. A whole-screen flip to blue means the underwater state is still a binary camera test rather than a per-ray path integral.",
   "regressions": [
    "over-under waterline"
   ],
   "camera": {
    "body": "earth",
    "lat": 31.5,
    "lon": 17.5,
    "altitude_km": -0.0255,
    "look_offset_deg": 88
   },
   "showcase": {
    "time": "11.0",
    "sea": "0.25"
   },
   "settle_s": 14,
   "hold_altitude": true
  },
  {
   "id": "snow-close",
   "desc": "Snowfall at eye level - drift speed and the procedural flake shape.",
   "expect": "Flakes drift slowly (near 1 m/s terminal, not accelerating) and near ones resolve as six-fold crystals rather than dots. Fast streaking sleet means gravity is accelerating them again.",
   "regressions": [
    "snow drift speed",
    "procedural snowflake"
   ],
   "camera": {
    "body": "earth",
    "lat": 35.3,
    "lon": 138.8,
    "altitude_km": 0.03,
    "look_offset_deg": 80
   },
   "showcase": {
    "time": "12.0",
    "weather": "snow"
   },
   "settle_s": 14
  },
  {
   "id": "ground-storm-inslab",
   "desc": "Ground under an active storm at LOCAL noon, camera inside/under the low deck. The weather + in-slab combination clear-sky vantages never exercise. Runs on the HIGH cloud tier: scripts/probe-sweep.js never sets a cloud quality and src/config.rs:739 defaults to \"high\", so this vantage exercises High, NOT Medium. MEDIUM HAS ZERO RIG COVERAGE - no vantage anywhere in this file selects it - which is why any change to the Medium path is unverifiable here and must not be made without adding its own storm + in-slab coverage first.",
   "expect": "A dark stormy sky with a low ragged deck overhead at LOCAL NOON. Rain-dimmed but clearly DAYLIT: terrain and horizon readable, ground colours identifiable. The scene stays recognizable as weather, not abstract shapes. (showcase `time` is a GLOBAL clock; local solar hour = time + lon/15, so lon 138.8 needs time 2.9 for local noon - see _clock_note.)",
   "regressions": [
    "NO screen-filling concentric rings or banded shells (v0.1069.0 clouds slab-bounds regression: storm lowered the deck, in-slab camera flipped shell_ratio, Medium path consumed params.w bounds for the first time and the march ran on collapsed bounds; caught live by the operator, reverted v0.1070.0)",
    "no giant lattice/mesh silhouette filling the view",
    "no inverted sky (stars below the horizon at ground level)",
    "NO stars or Milky Way visible through the sky at any hour - an overcast storm deck is optically opaque (observed at v0.1073)",
    "whole-frame mean luminance must be above 60 (22.0 at v0.1073, because global clock 12.0 at lon 138.8 was local ~21:00 and this gate was silently a NIGHT scene)",
    "NO flat rectangular slabs anywhere in the tree field - the mid-field vegetation is clearest here of any vantage (photographed at v0.1078: a row of pale grey vertical slabs with hard straight tops and hard vertical sides, repeating across the frame). Every card past the 3D-model ring must show a crown outline with sky through its edge",
    "Under 12-25 m/s storm wind the whole stand LEANS DOWNWIND: every crown visibly displaced from over its base, and every tree in frame leaning the SAME compass direction, displacement growing with tree height. NO upright calm-day forest under a storm, and NO fanning (no two crowns leaning in opposite directions). Before v0.1079 the foliage wind was a hardcoded OBJECT-space vector at 00-bindings-vertex.wgsl:594 with a zero-mean sway and no WeatherState wind-speed input, and each instance's random yaw (planet_chunks.rs:1883) rotated it, so trees swayed in random directions at one fixed calm-day amplitude. CURRENTLY FAILING at v0.1085 - do NOT read this line as green. The vertex wind branch is gated `material.params.z >= 19.5 && material.params.z < 20.5` (assets/shaders/pbr/00-bindings-vertex.wgsl:577), i.e. material type 20, the procedural-plant path, ONLY. The fir and pine photoscans are registered type 19 (src/lib.rs:9477, add_textured_material), so at this lat/elev roughly a quarter of the near stand (fir+pine weight 2.0 of the 8.2 that passes the gates at lat 35.3 / ~1245 m, more as the player climbs) stands perfectly upright under 18 m/s wind; and tree cards (packed bit 17), grass cards (bit 18) and every card past the 120 m model ring have no wind branch at all, so the entire mid and far field is frozen. Type 19 is SHARED with machines and furniture (src/engine/home_meshes.rs:561, src/engine/world_load.rs:502) so it cannot simply be renumbered to 20 - the fix is a per-material wind flag in the free params.w slot (the type-19 fragment branch forces emissive_strength = 0.0 at 90-fragment-main.wgsl:600, so params.w is unread there) plus a widened vertex gate. See the VEGETATION RUNGS block in docs/PRIORITIES.md, rung 0."
   ],
   "camera": {
    "body": "earth",
    "lat": 35.3,
    "lon": 138.8,
    "altitude_km": 0.03,
    "look_offset_deg": 80
   },
   "showcase": {
    "time": "2.9",
    "weather": "storm"
   },
   "hold_altitude": true,
   "settle_s": 75,
   "_settle_note": "15 -> 75 + hold_altitude at v0.1082 (critic finding on the wf_4cbd951f round). This is the IDENTICAL camera pose as fuji-forest-ground (0.03 km / 80 deg, same lat/lon; only the weather differs), and that vantage measured the scene still streaming at 25 s and settling ~40 s later - so this vantage's quoted 50.7 ms +/- 1.8 baseline was a half-streamed scene (fresh settled runs read 96.9/97.9 ms), and its unpinned walking player produced terrain-embedded captures (fidelity round, 2026-07-31). Any A/B judged here before v0.1082 compared half-built scenes."
  },
  {
   "id": "ground-snow-gpu",
   "desc": "Heavy snow on the EXPERIMENTAL GPU particle path (gpu_precip flip). The stride-mismatch class only manifests here, never on the default CPU path.",
   "expect": "Ordinary falling snow: small white flakes over the forest scene, ground and trees clearly visible. Identical in character to the CPU-path snow.",
   "regressions": [
    "NO giant colored spheres, blobs or streaks (BUG-050: the sim shader wrote 64-byte vec3-aligned records into the 52-byte packed vertex stream, so every instance after the first read shifted garbage; sizes picked up world coordinates, colors picked up neighbouring floats)",
    "flakes must be small and white, not cyan/magenta ellipses"
   ],
   "camera": {
    "body": "earth",
    "lat": 35.3,
    "lon": 138.8,
    "altitude_km": 0.03,
    "look_offset_deg": 80
   },
   "showcase": {
    "time": "12.0",
    "weather": "snow",
    "gpu_precip": "1"
   },
   "settle_s": 12
  },
  {
   "id": "orbit-snow-gate",
   "desc": "Earth from orbit with an active Snow condition: precipitation must NOT follow the camera into space. This class has recurred twice (v0.1064 low flight; 2026-07-31 the None-altitude hole from FTL).",
   "expect": "The blue marble in clean black space. Weather HUD says Snow, but ZERO flakes anywhere in frame: precipitation lives under the cloud deck.",
   "regressions": [
    "NO white flakes over the planet or the starfield (snow-in-space: surface_altitude_m None was defaulted to 0 m, so the tropopause gate passed in orbit)"
   ],
   "camera": {
    "body": "earth",
    "altitude_km": 12000
   },
   "showcase": {
    "weather": "snow",
    "gpu_precip": "1"
   },
   "settle_s": 12
  }
 ]
}
