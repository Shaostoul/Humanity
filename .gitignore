# ── Build artifacts ──
/target/
/HumanityOS.exe
*.old.exe

# ── Runtime data (local only, never committed) ──
/logs/*
!/logs/.gitkeep
/saves/*
!/saves/.gitkeep
/backups/*
!/backups/.gitkeep
/config.json

# ── Dependencies ──
node_modules/

# ── Logs ──
*.log

# ── Secrets / credentials ──
.env
.env.*
*.env
*.pem
*.key
*.p12
*.pfx
*.p8
*.ovpn
*.kdbx
*.secrets.json
**/id_rsa
**/id_ed25519
**/authorized_keys
**/known_hosts
**/private_key*
.ssh/
**/.ssh/
local-heron-admin.json

# ── Database artifacts ──
*.bak
*.backup
*.dump
*.sqlite-shm
*.sqlite-wal
*.db-shm
*.db-wal

# ── OS/editor noise ──
.DS_Store
Thumbs.db
*.swp
*.tmp

# ── Claude Code ──
.claude/worktrees/
