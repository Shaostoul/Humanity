# 2026-07-06 — Last Fable day: chat/servers handoff to Opus

Short, forward-looking narrative for the FIRST OPUS SESSION. The decision-by-decision
record is in `data/coordination/orchestrator_state.json` (newest at the bottom); the
next-action backlog is `docs/PRIORITIES.md` (the MODEL HANDOFF block at the top of
"Active focus"). This file is the arc summary.

## Context

The operator's Fable-5 access ends ~July 7; Opus takes over for weeks/months with
less compute headroom. The operator asked to spend the last Fable day FINISHING the
launch-critical everyday-use chat features and to leave gameplay for Opus. He also
observed (repeatedly) that multi-agent, security/encryption-dense workflows trip
Fable's dual-use safeguard and auto-switch the turn to Opus — so all of today's work
was done SOLO with plain feature-completeness framing (no offensive-security jargon,
no fan-out audit workflows). That is a deliberate downgrade-avoidance tactic, not a
limitation of the work.

## What shipped today (v0.712 → v0.714, all released + version-synced)

The theme was the operator's priority #1: CHAT "Servers" fully developed for daily use.
Before today, the saved-servers sidebar could ADD a server but the entries were inert
labels — the Add Server modal even PROMISED "switch by clicking its name" and
`ChatServer.url`'s own doc comment described click-to-reconnect, but neither was wired.

- **v0.712.0** — Saved servers are clickable to SWITCH: clicking one points
  `server_url` at it and reconnects with the same identity (mirrors the Connect
  button: `connect_with_kyber`, reset reconnect timers, clear messages, reload
  history). The active server shows in the success color with a "(current)" tag and
  isn't re-clickable. Each non-current server gets a small "x" to FORGET the bookmark.
- **v0.713.0** — Switching resets the active channel to `#general` (every relay seeds
  it) so switching to a server that lacks your previous channel/DM/group never shows
  an empty view. Matches the existing general-fallback used on leave-DM/disband-group.
- **v0.714.0** — Add Server accepts a BARE HOST: typing `server1.example.com` (no
  scheme) is treated as `https://<host>` for validation + saving, instead of silently
  greying out the Add button. The saved-servers surface is now a full loop:
  add → switch → use → forget.

Each Rust minor got its `just build-game` exe stamp (v0.712.1 / v0.713.1 / v0.714.1).

## What was verified (no code needed)

- **DMs and Groups panels are already mature** — unread dots, active-channel bar,
  right-click context menu (View Profile / Close Conversation), Send-DM from the user
  profile modal, Create/Join groups with per-group notifications. No gaps found.
- **Native defaults to the live VPS relay** — `server_url` defaults to
  `https://united-humanity.us`, so a fresh install reaches the operator's relay with
  zero config. The live relay answered `/health` (ok) and `/api/stats` reported the
  running commit == the just-pushed SHA, proving the deploy pipeline auto-rebuilds and
  restarts the relay on each push. Priority #2 ("access my relay from my PC") works.
- **Mod/admin command reference is complete + discoverable** — the composer "?" button
  opens a General/Moderator/Admin slash-command reference.

Prior Fable-stretch work this week (context): Files add/remove-on-server (v0.709 signed
delete endpoint + v0.710 native shared-files manager), the widget review (v0.711),
first-boot storage chooser + portable mode, the in-app file browser, chat markdown/links
+ 1:1 voice call answer/place/mute, and the page-access + fresh-install audits.

## Left for Opus (flagged in PRIORITIES)

1. **Mod/admin DEEP audit** — the command REFERENCE is complete, but confirm every
   handler actually works server-side and that the server OWNER is auto-recognized as
   admin when he connects. This is auth-adjacent (the kind of content that tripped the
   Fable downgrade), and Opus is unaffected, so it was deliberately deferred.
2. **DM/group daily-driver pass** — panels are mature but only field use surfaces the
   rough edges; some of that is crypto-adjacent.
3. **Gameplay** — machine info-windows + vehicle selector, volume containers. Held per
   the operator's explicit instruction.

## Operator field tests owed (need the GUI + live network, can't be done headless)

Two-server switching, the first-boot storage chooser (fresh + USB drive), voice calls
(two clients), chat attach + shared-files upload/remove.

## Release-signing backlog (operator-only)

v0.678 → v0.719 are UNSIGNED. Desktop auto-update only offers SIGNED releases, so run
`export HUMANITY_SIGNING_PASSPHRASE=… && just sign-release vX.Y.Z` for the versions you
want v0.421+ desktop users to receive. An AI/CI cannot sign (needs the passphrase).

## Evening addendum (same day): the chat-improvements batch (v0.715 → v0.719)

After a model reset the operator re-engaged with "get those chat improvements and
other stuff done" and re-allowed subagents. Two Explore scouts mapped (a) the native
DM receive path and (b) the complete relay slash-command surface; implementation was
done solo on main. Shipped:

- **v0.715** — DM list previews end-to-end: rows grow a muted elided preview line;
  incoming DMs update/create the sidebar entry (unread unless that conversation is
  open); own sends show "You: …"; opening clears unread. Snapshot-verified.
  IMPORTANT design note: the WEB DM sidebar deliberately stays name-only (operator
  2026-05-27 — its stored bodies are opaque E2EE envelopes); native previews are
  operator-approved 2026-07-06 and decrypt on arrival. chat-dms.js documents both
  so nobody reverts native for "parity."
- **v0.716** — command-audit fixes: the slash-command gate rejected any message
  containing a dot ANYWHERE, so `/server-add <url>` (URLs have dots) and
  `/report bob spam.` posted PUBLICLY instead of executing — now only the command
  word is dot-checked. `/friend-code` + `/redeem` gained real text handlers (they
  were documented everywhere but existed only as GUI enum messages). `/dm` removed
  from help docs (disabled since v0.279). 769 lib tests pass.
- **v0.717** — group unread dots (+ preserved across group_list rebuilds) and the
  admin-bootstrap comment fix: ADMIN_KEYS is deliberately the ONLY auto-admin path;
  the old comment claimed an unimplemented first-registered-user rule which would
  be a hostile-takeover vector on a public relay.
- **v0.718** — channel unread dots (+ preserved across channel_list rebuilds).
  P2P-group unread deliberately NOT built: native has no P2P push; closed P2P groups
  only poll their list, so there is no message event to flag. Waits for push.
- **v0.719** — the capstone: the Chat button in the top nav shows a theme.danger()
  dot whenever any DM/group/channel is unread — chat activity visible from EVERY
  page. (First draft used a color literal; theme_token_lint correctly failed it,
  fixed with the danger token.)

Audit verdict for the mod/admin question: all 40+ documented commands have working
relay handlers (scout-mapped to file:line); the operator IS admin on the live VPS
(ADMIN_KEYS set 2026-05-21, journaled at PRIORITIES:1280 — confirmed WITHOUT
touching prod after the permission classifier correctly refused an SSH read of the
live .env). Remaining idea for Opus: surface mod/admin actions as buttons.

## Night addendum: the game leg (v0.724 → v0.726.2)

The operator lifted the gameplay hold before leaving on errands ("focus on
non-game stuff first and then move forward with game stuff"). Shipped:

- **v0.724** — live machine walk-up cards: `MachineInstanceId` links ECS entities
  to HUD labels; a per-frame pass patches card stats from live components —
  cistern litres (plumbing sim), battery kWh (electrical sim), "low" under 15%.
  Previously the cards showed home.ron's static placeholders forever.
- **v0.725** — the assembler's infinite-of-X vehicle selector: the pinned card's
  "Auto-build:" dropdown lists same-station recipes.csv rows and rewrites the
  entity's AutoRefine; plus `assemble_nova` — the operator's 1975 Chevy Nova is
  now buildable at the factory (rover / pickup / Nova three-way choice).
- **v0.726** — material-storage Stage A slice 1: `volume_l` generated for all 496
  items (scripts/gen-item-volumes.js: mass / material density × per-category
  packing fraction), Inventory volume tracking every tick, a Volume tile beside
  Weight ("11.8 / 65 L", snapshot-verified) + per-item Volume detail rows.
- **v0.726.2** — the 7 missing material densities added + exactly their 19 item
  volumes recomputed (RECOMPUTE_MATS switch).

**Finding for the next session**: the typed-container system (containers.rs —
volume caps, content classes, damage/breakage, registry + tests) is complete but
has ZERO runtime callers — no placed machine spawns a Container component, so
"containers show contents" needs a design pass (which home.ron machines are
containers, how MachineDef declares container_type) before the card stat can read
anything.

## Late-night addendum: the container-fill burst (v0.728 → v0.729.1)

The operator (before signing off): use the remaining window on ACTUAL
development, no idle ticks, no redundant scanning. Shipped:

- **v0.728** — the container finding above got FIXED, not just filed:
  `MachineDef.container_type` (RON) → `spawn_home_machine_entity` inserts
  `Container::from_type` (first runtime use of the whole typed-container
  system). Grain silo = new `grain_silo_bin` archetype (4000 L,
  solid|dry_goods); fuel refinery + backup generator = `steel_fuel_drum`.
  Walk-up cards show live fill + a new "contents" row ("empty" /
  "120x Grain" / "BROKEN (spilled)").
- **v0.729** — first fill path: harvest surplus that doesn't fit the
  volume-gated pack routes into a compatible vessel instead of being lost
  (compatibility PRE-checked — `try_store` damages wrong-class vessels by
  design, and grain must not dent the fuel drum). `ItemDef` now parses the
  `content_class` column that existed unread since the container system was
  authored.
- **docs/game/model-pipeline.md** — GLB directive #7's guide half: authoring
  rules verified line-by-line against the real `load_gltf` (meters, Y-up,
  first-mesh-first-primitive, indexed triangles, auto normals/UVs, textures
  ignored), file locations, the `model:` field wiring plan, and the
  `replace_mesh`/shared-mesh-cache hazard that makes naive wiring corrupting.
- Also this evening: the texture-bug scout's "fix" was ADJUDICATED AND
  REFUTED (the shaders are canonical bilinear noise — journal decision #89
  has the real hypotheses), preventing an 11-shader regression.

**Still open (each mapped for its own session):** the `model:` field wiring
(renderer lifecycle), fuel-loop routing (refinery produces into / generator
consumes from their drums), vehicle BAY redesign, texture-bug in-game repro,
Stage-A follow-ons (per-vessel player deposits/withdrawals UI). Field tests
owed on: silo card fill after a full-pack harvest, all of the earlier list.
