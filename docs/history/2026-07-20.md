# 2026-07-20 - Overnight environment marathon (v0.906 - v0.910)

Operator directives (evening + overnight report): fix the "ocean is mostly
white" regression, organic sea with switchable states (glassy / ripples /
storm whitecaps per a reference photo), beach ground reading blue, plants
added + environment decorated (realistic, not cartoony), cloud density
contrast (dark cumulonimbus vs bright thin clouds), planet-landing distances
not fixed to Earth's, transitions that break/lock at fixed distances and
lose aim, every setting explained (low vs high) and actually working.

## Shipped

- **v0.906.0** - storm seas v1: de-squared sea variation (3 rotated
  octaves), MODIS-driven storm chop + whitecap foam. CC0 asset deliveries
  committed (ambientCG ground PBR textures, Poly Haven plant scans).
- **v0.907.0** - ground PBR textures wired: 8-layer triplanar array in the
  camera-relative planet-pinned domain, slope/imagery material classifier,
  normal-map perturbation; Settings sliders for sun shadows / god rays /
  SSAO; underwater depth tint + HUD readout; 41 dead quest ids fixed;
  sawmill + grain mill exist; plant repack script.
- **v0.908.0** - Europe-noon-darkness fix: Blue Marble vegetation measured
  ~20x darker than desert in linear light; hue-preserving shadow lift at
  bake (land_gain), cloud ground-shadow ceiling 0.5 -> 0.35, ground-texture
  desaturation before normalization (killed a warm-brown cast).
- **v0.909.0** - the big overnight batch:
  - Ocean whiteout ROOT-CAUSED: the detail-distance slider scaled animated
    wave/foam fades past their anti-alias bound into speckle. Animated
    water now fades on raw pixel coverage; foam is crest-only with a hard
    ~5 m/px reach. Probe-verified at the exact regression view.
  - SEA STATES 0..1: glassy mirror / classic ripples / storm (dark slate
    water, 2.3x chop, crest streaks) from the game wind + local MODIS storm
    cell; `showcase {"sea":"0..1"|"auto"}` pins it for testing.
  - Beaches keep sand color (ocean floor fades in over the first ~40 m of
    depth instead of snapping blue at 1 cm).
  - PLANTS IN-WORLD: material type 19 (textured mesh + alpha cutout),
    loader base-color texture decode, 17 per-variant photoscan files,
    data/entities/decorations.ron scatters 52 plants (fields, ferns, hero
    saplings, interior potted plant).
  - Cloud density contrast: regime tint spread widened (nimbostratus 0.42,
    cumulonimbus 0.55 vs cirrus/cumulus ~1.0) + column-opacity darkening.
  - Transitions: altitude bands scale with body radius (Moon ~2.7/27/273
    km, floors for asteroids), 6% edge hysteresis (no more boundary mode
    flapping = the "locked at fixed distances" feel), and up-vector changes
    preserve the WORLD look direction (the aim-loss root cause).
  - Settings truth pass: 23 hints added with low/high tradeoffs; wiring
    audit (40 live, 13 dead fields catalogued); VSync + Invert-Y wired for
    real. AudioManager discovered fully orphaned - audio integration queued.
- **v0.910.0** - altitude over water measured from the SEA SURFACE (was
  seafloor since the diving change: 100 m over a 3 km deep ocean read
  "Alt 3.1 km" and engaged the walk band 3 km early). Probe: 2.9 km ->
  7 m at the same spot.

## Found / queued (PRIORITIES updated)

Audio engine integration (no game sound exists), garden crop hero models
(Quaternius), cloud raymarch underside banding + deck-interior FPS,
grazing-angle texture anisotropy, forage flora spawns, quest Travel
emitters, Settings duplicate cards (Notifications/Wallet edit dead fields).

## Direction note - space-to-surface transitions

Tonight's fixes keep the banded model but make it planet-relative,
hysteretic, and aim-preserving. The durable end-state: dissolve the bands
into CONTINUOUS curves of normalized altitude (co-rotation weight, speed
governor, and up-vector blend already each blend across one band; extend
each curve over the whole range so no threshold exists anywhere), and hand
TRANSLATION to a sphere-of-influence frame hierarchy when we leave the
Earth locale. Logged in the journal.
