# 2026-07-25: the overnight backlog night (v0.955 - v0.966.x)

The operator filed three graphics field reports in the evening, answered two
pending calls (billboard sprites: bake our own; CC0 audio: approved), then at
about 01:00 handed over a ten-item backlog and went to bed. Everything below
shipped the same night, each as its own verified release.

## Evening: the field reports

- v0.955.0 trees-near-render fix: the vegetation biome gate demanded strictly
  green-dominant imagery; real forest texels are brown-green. Shared
  veg_biome_ok() now gates bake + harvest identically; gate-autopsy logging
  and two albedo regression tests added.
- v0.956.0 atmosphere limb: the daylight star-occlusion boost was forcing
  full opacity over terrain at the disc edge (southern Australia read as
  water). Occlusion now skips surface-hitting rays; australia-limb vantage
  guards it.
- v0.957.0 ocean wave height: water mesh depth cap 14 -> 17 (the 18-50 m
  chop became real geometry), Beaufort-4 amplitudes, shoal damping from the
  baked depth (vertex + CPU twin in lockstep).
- v0.958.0 horizon sheet band: root-caused via a new clouds on/off IPC knob -
  the band was the cloud deck seen edge-on with no aerial fade. Shared
  cloud_low_cam_haze() dissolves the deck past 30-80 km for low cameras.
- v0.959.0 billboard baker increment 1: automated model-to-sprite renderer
  (the Total Annihilation pipeline, in-engine); six conifer sprites in <1 s.
- v0.960.0 first game audio: 26 Kenney CC0 sounds, honest volume sliders,
  UI click through the catalog, decode-chain regression test.
- v0.961.0 sprite tree cards: the far LOD textures its cards from the baked
  atlas via a negative-uv transport; distant treelines are real silhouettes.

## Overnight: the ten-item backlog

1. v0.962.0 liftoff fix: blend-band Space thrust used world +Y (negative
   radial component in the southern hemisphere - the exact three-phase
   symptom reported); both fly paths now use the eased camera up, and
   below-cap blend-band flight was unfrozen. Two regression tests.
2. v0.963.0 dense forests: 100 -> 800 trees/cell (~16k/km^2); sprite cards
   made it nearly free (93.5 -> 87.8 fps from 4x to 8x).
3. v0.964.0 water near-field LOD: depth cap 17 -> 20 (~0.6 m vertices at the
   waterline), leaves 256 -> 512, far-ocean cost unchanged.
4. v0.965.0 LOD settings by item type: registry-driven Settings block, live
   water-detail slider (14-20), honest muted rows for unshipped categories.
5. v0.964.2 content-creation guides: ten zero-prior-knowledge docs
   (docs/user/creating/), workflow-written, spot-checked, 0 broken links.
6. v0.965.2 Library all-docs: 53 docs / 12 categories in the in-app Library.
7. v0.965.3 archived-tasks audit: 13 archives evaluated; top-10 survivors
   recorded (docs/history/2026-07-25-archived-tasks-audit.md); two flat-wrong
   TIER 2 claims corrected in place.
8. v0.966.0 particles live: the dormant particle system wired (billboard
   pipeline pair, floating-origin ride); leaf drift among trees + space
   motion dust.
9. v0.966.2 shader-organization design: why the megashader is one file (one
   module per pipeline, no include in WGSL) and the accepted source-split
   plan; implementation gets a fresh session under the v0.782 verification
   bar.
10. v0.966.3 homestead design: docs/design/homestead.md - house inside the
    greenhouse, 10 rooms on the existing wall model, furniture as
    machine-catalog entries, fixtures split the aggregate utility nodes,
    six increments (1-4 data-only).

## Notes for tomorrow

- Deploy transients: two VPS git-fetch timeouts to github.com (v0.958.0,
  v0.960.0 runs); both superseded by the next push. Watch for recurrence.
- Perf baseline: post-09:00 UTC probe fps read ~30 percent low
  environmentally (pre-particles exe measured identical); re-baseline before
  trusting comparisons.
- Operator morning items: the liftoff fix wants a real flight test; forests,
  water, particles, and the Library want eyeballs; homestead increment 1 is
  ready to build.
