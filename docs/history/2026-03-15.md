# 2026-03-15 — Project State Snapshot

Source: Claude (Sonnet 4.6) memory files — consolidated reference snapshot

## Machine Specs

Windows 10 Home 64-bit (10.0.19045). Intel i7-8700K (6 cores / 12 threads). 32 GB RAM. NVIDIA RTX 4070 (12 GB VRAM). 3x Samsung 980 PRO 2TB NVMe. PreSonus AudioBox USB. Logitech C922 webcam. 2560x1440 display. Brave browser primary.

Windows paths use backslash. Use `node` for scripting (not python). SSH alias `humanity-vps` for VPS (Debian Linux).

## Infrastructure

Server: server1.shaostoul.com (Debian 11). Relay port 3210 behind nginx. Web root: /var/www/humanity/. Repo: /opt/Humanity/. Service: humanity-relay.service. Rust 1.93.0 on server.

Deploy: push to main → GitHub Actions SSH → git pull → cargo build → rsync assets → restart relay → Deploy Bot announces.

## Feature State

Pages live: Chat, Dashboard, Home, Notes (encrypted, AES-256-GCM), Tasks (Fibonacci scope board), Calendar (recurring events).

Recent additions: PBKDF2 passphrase key derivation (600k iterations), encrypted identity backup/restore, BIP39 seed phrase display, theme toggle (light/dark), PWA service worker, keyboard shortcuts overlay, recurring calendar events, task detail drawer.

Task board: 65 tasks (29 done, 8 testing, 28 backlog).

## Fibonacci Scope Model

10-tier civilization scale for tasks: Self (1, red) → Mind (1, orange) → Hearth (2, yellow) → Circle (3, green) → Village (5, green) → City (8, blue, default) → Region (13, gray) → World (21, purple) → Solar (34, blue) → Cosmos (55, white). Stored as labels: "scope:city". Golden spiral aesthetic direction.

## Roadmap

Tier 1 (foundational): Identity Recovery via seed phrase, Federation (skeleton exists), Multi-device Key Sync.
Tier 2 (core OS): Task System (started), Calendar with RSVP, Skills + Reputation, Personal Data Store.
Tier 3 (civilization): Group Governance/Voting, Marketplace, Learning Paths.
Tier 4 (reach): PWA (quick win), Public API + Webhooks.

## Session Continuity

If starting fresh: read history files → check tasks API → check recent commits → check server health → resume from roadmap.

Backup locations: GitHub repo (full history), VPS /opt/Humanity (server copy + binary), VPS /var/www/humanity (live assets), external HDD (local backup).

Recovery devices: OnePlus 9 Pro phone, old laptop with GTX 1060, USB drives, 2TB external HDD.

## AI Handoff Notes

Stack: Rust/axum/tokio/SQLite backend, plain HTML+JS client (no build step, no npm), CSS custom properties for theming, Tauri desktop app, Ed25519 identity via WebCrypto.

Key patterns: monkey-patching handleMessage across chat-*.js files, three-way context isolation (channel/DM/group — must null others and re-render all three lists), multiple impl Storage blocks across storage/*.rs, relay unicast via target field.

Coding style: no build step, JSDoc explains WHY not WHAT, short direct responses, update task board after significant work.
