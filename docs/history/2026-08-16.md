# 2026-08-16: the navigation map

Operator asked for a tree of where everything connects plus a mismatched-
name hunt. Two parallel surveys (native nav, web nav) produced
docs/reference/ui-navigation-map.md: both trees, a naming rule (the nav
button label IS the page name), 15 mismatches fixed in v0.1144.0, 13
catalogued with recommendations. Standouts fixed: pages that never stated
their own name (Quests), the web tasks page speaking Quests vocabulary
while a sibling nav entry named Quests points elsewhere, tab titles Gear/
Journal for Inventory/Notes, the Profile no-highlight first open, the boot
dropdown offering the same page under two names, and the deploy never
placing homepage flavors. Open standouts: the Maps/Cosmos double variant,
the Quests fork, Chat-vs-Network, Play/Studio/download, Civilization near-
unreachable, icon collisions, eleven never-active GuiPage variants.

## v0.1145.0: the galaxy gets real (maps arc rung 1)

Operator picked maps. Shipped: stars-map.bin (HYG 109,400 stars, true
galactic positions in light-years, self-verifying generator + independent
Rust lock on Sirius/Vega), galaxy view rewritten on it (zoom-scaled
magnitude ladder, spectral colors, rings to 10,000 ly), the Cosmos
variant merged into Maps (one name at last), views renamed Solar System |
Galaxy | Night Sky, Quests/Tasks split (game vs real life; tutorial
chains now a Tasks guide panel), web chat titled Chat, Studio titled,
SERVERS rail. Maps ladder designed in docs/design/maps-ladder.md: OSM
planet GPS is the next rung.

## v0.1146.0: real roads and buildings (OSM rung 1)

The Maps Planet view: scripts/fetch-osm-region.mjs (Overpass -> HOSMREG1,
self-verifying 23 checks, mutation-proven gate, byte-deterministic) + a
GPS-style renderer (class-laddered roads, height-tinted footprints,
labels, scale bar, ODbL attribution). Demo: downtown Seattle, 8,966
roads, Rainier Square Tower at its real 259 m. Rust lock on the shipped
file. Snapshot is unmistakably Seattle. Also drafted: the Play/Characters
WHO-WHERE proposal (docs/design/play-characters.md) for operator review.
Next: extrude the same data into the 3D world.
