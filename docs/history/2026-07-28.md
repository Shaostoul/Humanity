# 2026-07-28

## Morning loop (operator at Sponsor-A-Can): wrap-planet-gen day

Goal set by the operator at breakfast: finish the planetary pillars
(clouds, terrain, water, weather, plants) so development can return to
the gameplay loops and the house build-out.

v0.1021.1 clouds coherent drift: the live MODIS envelope pins cloud
placement to real geography, but every texture domain drifted at the old
theatrical rate - interiors slid through their own pinned silhouettes at
~580 km/min (the operator's "structure moving a different way than the
details"). Drift slowed ~75x to jet-stream speed; one-constant revert if
the calmer sky reads wrong. Cirrus crown floor made regime-aware (thin
families no longer gray).

v0.1022.1 far-tree canopy sheet: the vegetation arc's foundation. Two
rig captures taught the design: vertical billboards are edge-on
invisible from altitude (v1 vanished at 12 km), and one tree per 220 m
cell is 1/800th of forest density. Clumps (crossed verticals plus a
horizontal canopy quad at crown height, imagery-colored, per-cell shade
jitter) at stride 1/4/16 bands out to 150 km, same cell hash as the
patch bake, worker rebuilds every 2 km of camera travel. Borneo from
12 km went from bare green sphere to continuous forest mottling;
12-33k clumps in one draw at 72 FPS unthrottled. Trees-from-altitude
closed; the square/circle card-boundary artifact buried.

## Afternoon: the homestead arc opens (v0.1023.1, increment 1 - the shell)

The showcase home becomes a house: 16 walls forming the accepted 10-room
program (entry, common, kitchen, pantry, hall, bedroom, bathroom,
wetroom, study, utility, workshop+annex), doors and greenhouse-facing
windows per the design rules, spawn in the mudroom, room-labelled zones.
The old materials/doors showcase moved whole into the commons as a
permanent teaching exhibit. Splice lesson: indexOf on an indented
closing bracket matched a SUBSTRING inside deeper-indented lines and
corrupted the file on the first pass - newline-anchor structural
patterns in generated-splice scripts.

## Afternoon-evening: the homestead ladder (v0.1023-v0.1028)

Increments 1-4 of the accepted homestead design shipped in one day:
the 10-room shell (showcase preserved in the commons), the 23-fixture
lighting plan, the solo plumbing suite (first HotWater producer in the
played variant, pinned pipe/wire gauges), and the 34-piece furniture
set (ported verbatim - the family file already carried it; inventory
before mirroring, twice proven). Plus mid-stream: wolves removed after
a live player death (shared state verified clean via SSH), far-tree
sheet v3 emergency rescale, fly-through clouds, date-line feather,
shore de-terracing, and the weather-water hyper-realism roadmap doc
from the operator Q&A.

## Afternoon: water FFT increment 1 + operator digest (v0.1029-v0.1030)

Operator checked in mid-errands with screenshots + a wide-ranging
message (model tiering, empty glossary, canopy squares, dense grass,
"take things to their maximum potential").

- **v0.1029.1 - FFT ocean increment 1** (docs/design/water-fft.md):
  `src/terrain/ocean_fft.rs` - JONSWAP spectrum, hermitian time
  evolution, radix-2 IFFT, deterministic from terrain_seed, normalized
  to the trains' RMS envelope. Design-doc CORRECTION found during
  implementation: the tile must DIVIDE the 64 m ground-anchor modulus
  (64 m tile), not be a multiple (256 m would jump a quarter tile per
  anchor snap). Triplanar radial^2 projection covers all latitudes;
  the VS does manual-bilinear textureLoad (group 3 binding 15,
  R32Float) mirroring the CPU twin line for line; buoyancy samples the
  SAME array the GPU tile uploaded that frame. Settings > Graphics
  "FFT ocean (experimental)", default off. Lockstep tests extended.
- **Far-tree canopy sheet -> default OFF** (same release). Operator
  verdict twice: "black squares in a grid". Sheet stays behind a
  Settings toggle for A/B; long-range trees will come from the
  impostor/instancing arc instead.
- **v0.1030.1 - glossary blank-dictionary fix.** Root cause: the
  loader only read data_dir()/glossary.json and silently degraded to
  an EMPTY dictionary on installs whose data dir lacks the file, while
  the exe embedded all 201 terms (embedded_data::GLOSSARY_JSON) the
  whole time. Loader now falls back to the embedded copy on read or
  parse failure; a new test holds the embedded copy to the same bar.
- **docs/ai/model-tiering.md** (new): the Sonnet/Opus 50% plan -
  data-volume packages (Wiktionary/WordNet full dictionary, real
  plants, i18n, glossary curation, tools/chemistry depth) behind hard
  scope walls; Fable orchestrates via Workflow model overrides.

## Late afternoon: the weather arc closes end-to-end (v0.1031-v0.1037)

Seven more releases on the loop while the operator ran errands:

- v0.1031.1 FFT inc 2: spectral slopes (i*k*h) + choppy-field Jacobian
  whitecaps packed into an Rgba32Float tile; the water FS swaps its
  texture-detail chop for physical FFT shading in FFT mode. Geometry
  stays vertical-only so the buoyancy twin is untouched.
- v0.1032.1 cloud wind-advection: every weather-map lookup (sky MODIS
  envelope, storm sea-state, CPU god-ray dim) rotates by a
  wind-integrated zonal angle; a fresh MODIS map eases it back over
  45 s. Answers the operator's "pinned clouds" question.
- v0.1033.1 precipitation: weather-driven rain/snow area emitter at
  the camera (wind-tilted dir_override, intensity rate_scale, defs
  data-driven).
- v0.1034.1 extreme-weather schema: data/weather/events.ron
  (thunderstorm/blizzard/tornado/meteor_shower) + validated registry,
  emitter refs cross-checked against particles.ron.
- v0.1035.1 events LIVE: 900 s rolls, rarity-weighted eligibility,
  Front gusts on exported wind, HUD shows the event name, event
  emitters union into precipitation.
- v0.1036.1 rain streaks: velocity-stretched billboards via a
  per-def streak_stretch seconds field; stretch 0 = byte-identical
  round sprites.
- v0.1037.1 event cloud overrides: coverage_boost + tint ease onto
  the cloud material slots the shader already documented as
  tint/coverage - zero new plumbing.

Deferred rungs recorded in PRIORITIES: Vortex spatial wind + hazard
damage, cascade B + geometric chop (FFT inc 3, operator eyes first),
froxels + wind field, rivers, instanced grass (fresh session).
