//! File watcher — monitors asset directories for changes via notify.

/// Watches asset directories and emits reload events.
pub struct FileWatcher {
    // TODO: notify::RecommendedWatcher, event channel
}

impl FileWatcher {
    pub fn new() -> Self {
        Self {}
    }
}
