[package]
name = "module-electrical-basics"
version = "0.1.0"
edition = "2024"
description = "Electrical load/fault simulation primitives"
license = "CC0-1.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
