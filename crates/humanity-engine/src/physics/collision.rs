//! Collision event handling — processes Rapier contact/intersection events.

/// Processes collision events from the physics step.
pub struct CollisionHandler {
    // TODO: event queue, callback registry
}

impl CollisionHandler {
    pub fn new() -> Self {
        Self {}
    }
}
