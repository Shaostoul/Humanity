[package]
name = "core-session-orchestrator"
version = "0.1.0"
edition = "2024"
description = "Session mode + authority policy orchestration for Humanity"
license = "CC0-1.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
