//! Skill progression — learning-by-doing system.
//!
//! Skill definitions loaded from `data/skills.csv`.

pub mod learning;

/// Skill progression manager.
pub struct SkillSystem {
    // TODO: skill registry, player skill levels
}

impl SkillSystem {
    pub fn new() -> Self {
        Self {}
    }
}
