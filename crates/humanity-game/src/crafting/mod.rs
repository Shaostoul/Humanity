//! Recipe system — crafting recipes loaded from `data/recipes.csv`.

pub mod workstations;

/// Manages crafting recipes and in-progress crafts.
pub struct CraftingSystem {
    // TODO: recipe registry, active crafts
}

impl CraftingSystem {
    pub fn new() -> Self {
        Self {}
    }
}
