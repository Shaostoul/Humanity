[package]
name = "core-planet-icosphere"
version = "0.1.0"
edition = "2024"
description = "Icosphere planet LOD math and patch selection primitives"
license = "CC0-1.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
